class DateUtils {
  /**
   * Toma la Fecha de Hoy y genera un objeto bajo los formatos definidos
   * @returns  Objeto con la fecha y la hora en el formato standar
   * fecha: yyyy-mm-dd
   * hora: hh:mm
   */
  static getFechaHoraActual(): string {
    return '';
  }

  static getRegistrosEntreFechas(fechaDesde, fechaHasta): registrosTotales;

  static getRegistrosFaltantes(registrosTotales, resgistrosExistentes): registrosFaltantes;
}

// crear una interface para representar
// fecha: yyyy-mm-dd
// hora: hh:mm

export default DateUtils;
